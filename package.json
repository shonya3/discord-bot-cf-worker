{
  "name": "cf-discord-bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "commands": "tsx scripts/commands-register.ts",
    "update-url": "tsx scripts/update-interactions-url.ts",
    "dev": "tsx scripts/start.ts",
    "deploy": "tsx scripts/deploy.ts",
    "cf-typegen": "wrangler types",
    "check": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "fmt": "oxfmt"
  },
  "dependencies": {
    "dotenv": "^17.4",
    "hono": "4.12.19"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "discord-interactions": "^4.4",
    "ngrok": "^5.0.0-beta.2",
    "oxfmt": "^0.50.0",
    "tsx": "^4.22.2",
    "typescript": "^6.0",
    "vitest": "^4.1",
    "wrangler": "4.92.0"
  }
}
